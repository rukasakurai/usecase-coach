# Reference use-case structure — design notes

Accompanies [`schema.json`](./schema.json).

A reference use case is a flat JSON object with three required string fields:

- `problem` — the pain-point.
- `approach` — how it was tackled.
- `impact` — the material difference produced.

